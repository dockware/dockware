name: play (All)

on:
  workflow_dispatch:

jobs:
  
    play-latest: 
      name: Release play:latest
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=latest -B
          
        - name: Build Image
          run: make build image=play tag=latest -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=latest -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_latest
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:latest
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: latest

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_latest
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/latest
            platforms: linux/amd64
            push: false
            tags: dockware/play:latest
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/latest
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:latest
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/latest
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:latest

  
    play-6-4-19-0: 
      name: Release play:6.4.19.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.19.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.19.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.19.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.19.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.19.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.19.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.19.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.19.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.19.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.19.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.19.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.19.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.19.0

  
    play-6-4-18-1: 
      name: Release play:6.4.18.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.18.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.18.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.18.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.18.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.18.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.18.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.18.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.18.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.18.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.18.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.18.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.18.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.18.1

  
    play-6-4-18-0: 
      name: Release play:6.4.18.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.18.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.18.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.18.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.18.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.18.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.18.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.18.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.18.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.18.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.18.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.18.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.18.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.18.0

  
    play-6-4-17-2: 
      name: Release play:6.4.17.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.17.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.17.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.17.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.17.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.17.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.17.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.17.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.17.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.17.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.17.2

  
    play-6-4-17-1: 
      name: Release play:6.4.17.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.17.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.17.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.17.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.17.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.17.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.17.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.17.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.17.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.17.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.17.1

  
    play-6-4-17-0: 
      name: Release play:6.4.17.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.17.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.17.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.17.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.17.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.17.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.17.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.17.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.17.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.17.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.17.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.17.0

  
    play-6-4-16-1: 
      name: Release play:6.4.16.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.16.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.16.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.16.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.16.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.16.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.16.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.16.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.16.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.16.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.16.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.16.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.16.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.16.1

  
    play-6-4-16-0: 
      name: Release play:6.4.16.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.16.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.16.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.16.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.16.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.16.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.16.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.16.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.16.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.16.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.16.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.16.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.16.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.16.0

  
    play-6-4-15-2: 
      name: Release play:6.4.15.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.15.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.15.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.15.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.15.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.15.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.15.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.15.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.15.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.15.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.15.2

  
    play-6-4-15-1: 
      name: Release play:6.4.15.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.15.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.15.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.15.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.15.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.15.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.15.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.15.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.15.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.15.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.15.1

  
    play-6-4-15-0: 
      name: Release play:6.4.15.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.15.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.15.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.15.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.15.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.15.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.15.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.15.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.15.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.15.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.15.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.15.0

  
    play-6-4-14-0: 
      name: Release play:6.4.14.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.14.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.14.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.14.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.14.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.14.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.14.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.14.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.14.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.14.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.14.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.14.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.14.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.14.0

  
    play-6-4-13-0: 
      name: Release play:6.4.13.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.13.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.13.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.13.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.13.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.13.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.13.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.13.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.13.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.13.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.13.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.13.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.13.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.13.0

  
    play-6-4-12-0: 
      name: Release play:6.4.12.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.12.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.12.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.12.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.12.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.12.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.12.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.12.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.12.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.12.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.12.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.12.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.12.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.12.0

  
    play-6-4-11-1: 
      name: Release play:6.4.11.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.11.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.11.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.11.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.11.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.11.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.11.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.11.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.11.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.11.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.11.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.11.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.11.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.11.1

  
    play-6-4-11-0: 
      name: Release play:6.4.11.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.11.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.11.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.11.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.11.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.11.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.11.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.11.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.11.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.11.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.11.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.11.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.11.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.11.0

  
    play-6-4-10-1: 
      name: Release play:6.4.10.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.10.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.10.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.10.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.10.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.10.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.10.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.10.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.10.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.10.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.10.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.10.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.10.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.10.1

  
    play-6-4-10-0: 
      name: Release play:6.4.10.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.10.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.10.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.10.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.10.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.10.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.10.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.10.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.10.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.10.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.10.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.10.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.10.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.10.0

  
    play-6-4-9-0: 
      name: Release play:6.4.9.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.9.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.9.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.9.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.9.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.9.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.9.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.9.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.9.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.9.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.9.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.9.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.9.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.9.0

  
    play-6-4-8-2: 
      name: Release play:6.4.8.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.8.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.8.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.8.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.8.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.8.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.8.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.8.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.8.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.8.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.8.2

  
    play-6-4-8-1: 
      name: Release play:6.4.8.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.8.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.8.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.8.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.8.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.8.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.8.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.8.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.8.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.8.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.8.1

  
    play-6-4-8-0: 
      name: Release play:6.4.8.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.8.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.8.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.8.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.8.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.8.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.8.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.8.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.8.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.8.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.8.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.8.0

  
    play-6-4-7-0: 
      name: Release play:6.4.7.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.7.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.7.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.7.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.7.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.7.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.7.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.7.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.7.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.7.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.7.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.7.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.7.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.7.0

  
    play-6-4-6-1: 
      name: Release play:6.4.6.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.6.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.6.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.6.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.6.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.6.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.6.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.6.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.6.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.6.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.6.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.6.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.6.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.6.1

  
    play-6-4-6-0: 
      name: Release play:6.4.6.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.6.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.6.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.6.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.6.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.6.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.6.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.6.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.6.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.6.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.6.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.6.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.6.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.6.0

  
    play-6-4-5-1: 
      name: Release play:6.4.5.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.5.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.5.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.5.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.5.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.5.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.5.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.5.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.5.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.5.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.5.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.5.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.5.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.5.1

  
    play-6-4-5-0: 
      name: Release play:6.4.5.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.5.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.5.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.5.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.5.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.5.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.5.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.5.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.5.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.5.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.5.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.5.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.5.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.5.0

  
    play-6-4-4-1: 
      name: Release play:6.4.4.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.4.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.4.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.4.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.4.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.4.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.4.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.4.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.4.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.4.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.4.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.4.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.4.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.4.1

  
    play-6-4-4-0: 
      name: Release play:6.4.4.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.4.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.4.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.4.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.4.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.4.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.4.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.4.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.4.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.4.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.4.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.4.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.4.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.4.0

  
    play-6-4-3-1: 
      name: Release play:6.4.3.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.3.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.3.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.3.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.3.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.3.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.3.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.3.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.3.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.3.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.3.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.3.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.3.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.3.1

  
    play-6-4-3-0: 
      name: Release play:6.4.3.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.3.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.3.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.3.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.3.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.3.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.3.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.3.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.3.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.3.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.3.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.3.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.3.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.3.0

  
    play-6-4-2-1: 
      name: Release play:6.4.2.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.2.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.2.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.2.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.2.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.2.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.2.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.2.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.2.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.2.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.2.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.2.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.2.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.2.1

  
    play-6-4-2-0: 
      name: Release play:6.4.2.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.2.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.2.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.2.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.2.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.2.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.2.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.2.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.2.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.2.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.2.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.2.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.2.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.2.0

  
    play-6-4-1-2: 
      name: Release play:6.4.1.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.1.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.1.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.1.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.1.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.1.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.1.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.1.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.1.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.1.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.1.2

  
    play-6-4-1-1: 
      name: Release play:6.4.1.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.1.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.1.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.1.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.1.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.1.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.1.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.1.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.1.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.1.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.1.1

  
    play-6-4-1-0: 
      name: Release play:6.4.1.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.1.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.1.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.1.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.1.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.1.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.1.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.1.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.1.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.1.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.1.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.1.0

  
    play-6-4-0-0: 
      name: Release play:6.4.0.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.0.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.0.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.0.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.0.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.0.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.0.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.0.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.0.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.0.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.0.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.0.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.0.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.0.0

  
    play-6-4-0-0-rc1: 
      name: Release play:6.4.0.0-rc1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.4.0.0-rc1 -B
          
        - name: Build Image
          run: make build image=play tag=6.4.0.0-rc1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.4.0.0-rc1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.4.0.0-rc1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.4.0.0-rc1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.4.0.0-rc1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.4.0.0-rc1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.0.0-rc1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.4.0.0-rc1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.0.0-rc1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.4.0.0-rc1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.4.0.0-rc1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.4.0.0-rc1

  
    play-6-3-5-4: 
      name: Release play:6.3.5.4
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.5.4 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.5.4 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.5.4 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.5.4
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.5.4
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.5.4

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.5.4
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.4
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.5.4
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.4
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.5.4
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.4
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.5.4

  
    play-6-3-5-3: 
      name: Release play:6.3.5.3
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.5.3 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.5.3 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.5.3 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.5.3
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.5.3
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.5.3

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.5.3
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.3
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.5.3
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.3
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.5.3
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.3
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.5.3

  
    play-6-3-5-2: 
      name: Release play:6.3.5.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.5.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.5.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.5.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.5.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.5.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.5.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.5.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.5.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.5.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.5.2

  
    play-6-3-5-1: 
      name: Release play:6.3.5.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.5.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.5.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.5.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.5.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.5.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.5.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.5.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.5.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.5.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.5.1

  
    play-6-3-5-0: 
      name: Release play:6.3.5.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.5.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.5.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.5.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.5.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.5.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.5.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.5.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.5.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.5.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.5.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.5.0

  
    play-6-3-4-1: 
      name: Release play:6.3.4.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.4.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.4.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.4.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.4.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.4.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.4.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.4.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.4.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.4.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.4.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.4.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.4.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.4.1

  
    play-6-3-4-0: 
      name: Release play:6.3.4.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.4.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.4.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.4.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.4.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.4.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.4.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.4.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.4.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.4.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.4.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.4.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.4.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.4.0

  
    play-6-3-3-1: 
      name: Release play:6.3.3.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.3.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.3.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.3.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.3.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.3.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.3.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.3.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.3.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.3.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.3.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.3.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.3.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.3.1

  
    play-6-3-3-0: 
      name: Release play:6.3.3.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.3.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.3.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.3.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.3.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.3.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.3.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.3.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.3.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.3.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.3.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.3.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.3.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.3.0

  
    play-6-3-2-1: 
      name: Release play:6.3.2.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.2.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.2.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.2.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.2.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.2.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.2.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.2.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.2.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.2.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.2.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.2.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.2.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.2.1

  
    play-6-3-2-0: 
      name: Release play:6.3.2.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.2.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.2.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.2.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.2.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.2.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.2.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.2.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.2.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.2.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.2.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.2.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.2.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.2.0

  
    play-6-3-1-1: 
      name: Release play:6.3.1.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.1.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.1.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.1.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.1.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.1.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.1.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.1.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.1.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.1.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.1.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.1.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.1.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.1.1

  
    play-6-3-1-0: 
      name: Release play:6.3.1.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.1.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.1.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.1.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.1.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.1.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.1.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.1.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.1.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.1.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.1.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.1.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.1.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.1.0

  
    play-6-3-0-2: 
      name: Release play:6.3.0.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.0.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.0.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.0.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.0.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.0.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.0.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.0.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.0.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.0.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.0.2

  
    play-6-3-0-1: 
      name: Release play:6.3.0.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.0.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.0.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.0.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.0.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.0.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.0.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.0.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.0.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.0.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.0.1

  
    play-6-3-0-0: 
      name: Release play:6.3.0.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.3.0.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.3.0.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.3.0.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.3.0.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.3.0.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.3.0.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.3.0.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.3.0.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.3.0.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.3.0.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.3.0.0

  
    play-6-2-3: 
      name: Release play:6.2.3
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.2.3 -B
          
        - name: Build Image
          run: make build image=play tag=6.2.3 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.2.3 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.2.3
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.2.3
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.2.3

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.2.3
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.3
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.2.3
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.3
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.2.3
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.3
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.2.3

  
    play-6-2-2: 
      name: Release play:6.2.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.2.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.2.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.2.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.2.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.2.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.2.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.2.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.2.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.2.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.2.2

  
    play-6-2-1: 
      name: Release play:6.2.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.2.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.2.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.2.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.2.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.2.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.2.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.2.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.2.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.2.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.2.1

  
    play-6-2-0: 
      name: Release play:6.2.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.2.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.2.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.2.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.2.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.2.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.2.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.2.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.2.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.2.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.2.0

  
    play-6-2-0-rc1: 
      name: Release play:6.2.0-rc1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.2.0-rc1 -B
          
        - name: Build Image
          run: make build image=play tag=6.2.0-rc1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.2.0-rc1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.2.0-rc1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.2.0-rc1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.2.0-rc1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.2.0-rc1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.0-rc1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.2.0-rc1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.0-rc1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.2.0-rc1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.2.0-rc1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.2.0-rc1

  
    play-6-1-6: 
      name: Release play:6.1.6
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.1.6 -B
          
        - name: Build Image
          run: make build image=play tag=6.1.6 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.1.6 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.1.6
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.1.6
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.1.6

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.1.6
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.6
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.1.6
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.6
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.1.6
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.6
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.1.6

  
    play-6-1-5: 
      name: Release play:6.1.5
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.1.5 -B
          
        - name: Build Image
          run: make build image=play tag=6.1.5 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.1.5 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.1.5
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.1.5
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.1.5

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.1.5
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.5
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.1.5
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.5
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.1.5
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.5
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.1.5

  
    play-6-1-4: 
      name: Release play:6.1.4
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.1.4 -B
          
        - name: Build Image
          run: make build image=play tag=6.1.4 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.1.4 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.1.4
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.1.4
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.1.4

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.1.4
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.4
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.1.4
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.4
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.1.4
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.4
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.1.4

  
    play-6-1-3: 
      name: Release play:6.1.3
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.1.3 -B
          
        - name: Build Image
          run: make build image=play tag=6.1.3 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.1.3 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.1.3
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.1.3
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.1.3

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.1.3
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.3
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.1.3
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.3
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.1.3
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.3
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.1.3

  
    play-6-1-2: 
      name: Release play:6.1.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.1.2 -B
          
        - name: Build Image
          run: make build image=play tag=6.1.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.1.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.1.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.1.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.1.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.1.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.1.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.1.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.1.2

  
    play-6-1-1: 
      name: Release play:6.1.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.1.1 -B
          
        - name: Build Image
          run: make build image=play tag=6.1.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.1.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.1.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.1.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.1.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.1.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.1.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.1.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.1.1

  
    play-6-1-0: 
      name: Release play:6.1.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=6.1.0 -B
          
        - name: Build Image
          run: make build image=play tag=6.1.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=6.1.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_6.1.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:6.1.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 6.1.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_6.1.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:6.1.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:6.1.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/6.1.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:6.1.0

  
    play-5-7-16: 
      name: Release play:5.7.16
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.16 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.16 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.16 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.16
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.16
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.16

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.16
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.16
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.16
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.16
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.16
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.16
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.16

  
    play-5-7-16-rc: 
      name: Release play:5.7.16-rc
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.16-rc -B
          
        - name: Build Image
          run: make build image=play tag=5.7.16-rc -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.16-rc -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.16-rc
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.16-rc
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.16-rc

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.16-rc
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.16-rc
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.16-rc
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.16-rc
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.16-rc
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.16-rc
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.16-rc

  
    play-5-7-15: 
      name: Release play:5.7.15
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.15 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.15 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.15 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.15
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.15
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.15

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.15
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.15
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.15
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.15
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.15
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.15
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.15

  
    play-5-7-14: 
      name: Release play:5.7.14
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.14 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.14 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.14 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.14
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.14
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.14

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.14
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.14
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.14
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.14
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.14
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.14
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.14

  
    play-5-7-13: 
      name: Release play:5.7.13
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.13 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.13 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.13 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.13
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.13
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.13

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.13
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.13
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.13
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.13
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.13
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.13
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.13

  
    play-5-7-12: 
      name: Release play:5.7.12
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.12 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.12 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.12 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.12
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.12
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.12

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.12
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.12
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.12
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.12
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.12
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.12
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.12

  
    play-5-7-11: 
      name: Release play:5.7.11
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.11 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.11 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.11 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.11
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.11
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.11

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.11
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.11
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.11
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.11
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.11
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.11
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.11

  
    play-5-7-10: 
      name: Release play:5.7.10
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.10 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.10 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.10 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.10
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.10
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.10

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.10
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.10
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.10
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.10
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.10
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.10
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.10

  
    play-5-7-9: 
      name: Release play:5.7.9
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.9 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.9 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.9 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.9
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.9
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.9

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.9
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.9
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.9
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.9
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.9
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.9
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.9

  
    play-5-7-8: 
      name: Release play:5.7.8
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.8 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.8 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.8 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.8
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.8
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.8

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.8
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.8
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.8
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.8
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.8
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.8
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.8

  
    play-5-7-7: 
      name: Release play:5.7.7
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.7 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.7 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.7 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.7
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.7
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.7

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.7
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.7
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.7
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.7
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.7
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.7
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.7

  
    play-5-7-6: 
      name: Release play:5.7.6
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.6 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.6 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.6 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.6
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.6
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.6

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.6
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.6
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.6
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.6
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.6
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.6
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.6

  
    play-5-7-5: 
      name: Release play:5.7.5
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.5 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.5 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.5 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.5
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.5
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.5

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.5
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.5
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.5
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.5
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.5
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.5
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.5

  
    play-5-7-4: 
      name: Release play:5.7.4
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.4 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.4 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.4 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.4
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.4
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.4

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.4
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.4
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.4
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.4
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.4
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.4
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.4

  
    play-5-7-3: 
      name: Release play:5.7.3
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.3 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.3 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.3 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.3
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.3
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.3

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.3
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.3
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.3
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.3
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.3
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.3
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.3

  
    play-5-7-2: 
      name: Release play:5.7.2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.2 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.2
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.2

  
    play-5-7-1: 
      name: Release play:5.7.1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.1 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.1
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.1

  
    play-5-7-0: 
      name: Release play:5.7.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.0 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.0

  
    play-5-7-0-rc2: 
      name: Release play:5.7.0-rc2
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.0-rc2 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.0-rc2 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.0-rc2 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.0-rc2
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.0-rc2
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.0-rc2

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.0-rc2
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0-rc2
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.0-rc2
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0-rc2
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.0-rc2
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0-rc2
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.0-rc2

  
    play-5-7-0-rc1: 
      name: Release play:5.7.0-rc1
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.7.0-rc1 -B
          
        - name: Build Image
          run: make build image=play tag=5.7.0-rc1 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.7.0-rc1 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.7.0-rc1
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.7.0-rc1
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.7.0-rc1

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.7.0-rc1
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0-rc1
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.7.0-rc1
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0-rc1
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.7.0-rc1
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.7.0-rc1
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.7.0-rc1

  
    play-5-6-10: 
      name: Release play:5.6.10
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.6.10 -B
          
        - name: Build Image
          run: make build image=play tag=5.6.10 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.6.10 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.6.10
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.6.10
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.6.10

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.6.10
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.10
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.6.10
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.10
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.6.10
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.10
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.6.10

  
    play-5-6-9: 
      name: Release play:5.6.9
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.6.9 -B
          
        - name: Build Image
          run: make build image=play tag=5.6.9 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.6.9 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.6.9
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.6.9
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.6.9

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.6.9
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.9
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.6.9
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.9
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.6.9
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.9
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.6.9

  
    play-5-6-8: 
      name: Release play:5.6.8
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.6.8 -B
          
        - name: Build Image
          run: make build image=play tag=5.6.8 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.6.8 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.6.8
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.6.8
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.6.8

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.6.8
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.8
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.6.8
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.8
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.6.8
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.8
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.6.8

  
    play-5-6-7: 
      name: Release play:5.6.7
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.6.7 -B
          
        - name: Build Image
          run: make build image=play tag=5.6.7 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.6.7 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.6.7
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.6.7
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.6.7

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.6.7
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.7
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.6.7
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.7
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.6.7
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.7
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.6.7

  
    play-5-6-6: 
      name: Release play:5.6.6
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.6.6 -B
          
        - name: Build Image
          run: make build image=play tag=5.6.6 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.6.6 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.6.6
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.6.6
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.6.6

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.6.6
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.6
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.6.6
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.6
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.6.6
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.6
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.6.6

  
    play-5-6-4: 
      name: Release play:5.6.4
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.6.4 -B
          
        - name: Build Image
          run: make build image=play tag=5.6.4 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.6.4 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.6.4
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.6.4
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.6.4

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.6.4
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.4
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.6.4
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.4
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.6.4
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.4
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.6.4

  
    play-5-6-3: 
      name: Release play:5.6.3
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.6.3 -B
          
        - name: Build Image
          run: make build image=play tag=5.6.3 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.6.3 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.6.3
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.6.3
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.6.3

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.6.3
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.3
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.6.3
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.3
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.6.3
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.6.3
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.6.3

  
    play-5-5-10: 
      name: Release play:5.5.10
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.5.10 -B
          
        - name: Build Image
          run: make build image=play tag=5.5.10 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.5.10 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.5.10
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.5.10
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.5.10

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.5.10
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.5.10
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.5.10
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.5.10
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.5.10
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.5.10
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.5.10

  
    play-5-5-0: 
      name: Release play:5.5.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.5.0 -B
          
        - name: Build Image
          run: make build image=play tag=5.5.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.5.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.5.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.5.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.5.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.5.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.5.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.5.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.5.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.5.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.5.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.5.0

  
    play-5-4-6: 
      name: Release play:5.4.6
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.4.6 -B
          
        - name: Build Image
          run: make build image=play tag=5.4.6 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.4.6 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.4.6
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.4.6
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.4.6

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.4.6
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.4.6
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.4.6
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.4.6
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.4.6
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.4.6
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.4.6

  
    play-5-3-0: 
      name: Release play:5.3.0
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.3.0 -B
          
        - name: Build Image
          run: make build image=play tag=5.3.0 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.3.0 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.3.0
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.3.0
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.3.0

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.3.0
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.3.0
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.3.0
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.3.0
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.3.0
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.3.0
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.3.0

  
    play-5-2-21: 
      name: Release play:5.2.21
      runs-on: ubuntu-latest
      continue-on-error: true
      steps:
        - name: Clone Code
          uses: actions/checkout@v2
    
        - name: Install Dependencies
          run: make install
    
        - name: ORCA Generate
          run: make generate -B
    
        - name: Verify Configuration
          run: make verify image=play tag=5.2.21 -B
          
        - name: Build Image
          run: make build image=play tag=5.2.21 -B

        - name: Run SVRUnit Tests
          run: make test image=play tag=5.2.21 -B
          
        - name: Store SVRUnit Report
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: svrunit_report_play_5.2.21
            retention-days: 3
            path: |
                .reports
        
        - name: Build JUnit Report
          uses: dorny/test-reporter@v1
          if: always()
          with:
            name: SVRUnit Tests
            path: .reports/report.xml
            reporter: jest-junit
                    
        - name: Start Image
          run: |
            docker run --rm -p 80:80 --name shop -d dockware/play:5.2.21
            sleep 30
            docker logs shop

        - name: Install Cypress
          run: cd tests/cypress && make install -B

        - name: Run Cypress Tests
          run: |
            if [[ $DW_IMAGE == flex ]]; then
               cd tests/cypress && make run-flex url=http://localhost
            fi
            if [[ $DW_IMAGE == essentials ]]; then
               cd tests/cypress && make run-essentials url=http://localhost
            fi
            if [[ $DW_IMAGE == play && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == 6.5.0.0-rc1 ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.5.0.0
            fi
            if [[ $DW_IMAGE == dev && $SW_VERSION == latest ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=6.4.19.0
            fi
            if [[ $SW_VERSION == 6.* ]]; then
               cd tests/cypress && make run6 url=http://localhost shopware=$SW_VERSION
            fi
            if [[ $SW_VERSION == 5.* ]]; then
               cd tests/cypress && make run5 url=http://localhost shopware=$SW_VERSION
            fi
          env:
            DW_IMAGE: play
            SW_VERSION: 5.2.21

        - name: Store Cypress Results
          uses: actions/upload-artifact@v2
          if: always()
          with:
            name: cypress_results_play_5.2.21
            retention-days: 1
            path: |
              Tests/Cypress/cypress/videos
              Tests/Cypress/cypress/screenshots     

        - name: Set up Docker Buildx
          id: buildx
          uses: docker/setup-buildx-action@v1
        
        - name: Login to Docker Hub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PASSWORD }}
        
        - name: Build amd64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.2.21
            platforms: linux/amd64
            push: false
            tags: dockware/play:5.2.21
            
        - name: Build arm64
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.2.21
            platforms: linux/arm64/v8
            push: false
            tags: dockware/play:5.2.21
            
        - name: Build and push multiarch
          uses: docker/build-push-action@v2
          with:
            context: ./dist/images/play/5.2.21
            platforms: linux/amd64,linux/arm64/v8
            push: true
            tags: dockware/play:5.2.21
