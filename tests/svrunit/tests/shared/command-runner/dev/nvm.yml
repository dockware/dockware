commands:
  - name: "NVM is installed and loaded"
    command: "nvm --version"
    expected: "0.39.1"
  - name: "NVM can switch Node version"
    command: "nvm use default"
    expected: "Now using node v"