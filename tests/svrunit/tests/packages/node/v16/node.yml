commands:
  - name: "NODE Version 16"
    command: "node -v"
    expected: "v16."
  - name: "NODE Version 16 as SUDO user"
    command: "sudo node -v"
    not_expected: "command not found" # sudo version switch does not always work, but it needs to be at least found!!!