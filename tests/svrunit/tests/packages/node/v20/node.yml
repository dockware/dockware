commands:
  - name: "NODE Version 20"
    command: "node -v"
    expected: "v20."
  - name: "NODE Version 20 as SUDO user"
    command: "sudo node -v"
    not_expected: "command not found" # sudo version switch does not always work, but it needs to be at least found!!!