    && apt-get install -y build-essential software-properties-common \
    && apt-get install -y ruby-dev \
    && gem install mailcatcher --conservative --no-force \
    #&& echo which mailchatcher \
    #&& phpenmod mailcatcher \
